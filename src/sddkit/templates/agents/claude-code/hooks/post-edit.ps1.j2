# post-edit.ps1 — runs after Claude edits any file
# Generated by spec-builder

param([string]$InputJson = "")

if (-not $InputJson) {
    $InputJson = $input | Out-String
}

# Extract file path
$File = [regex]::Match($InputJson, '"path":"([^"]*)"').Groups[1].Value

if (-not $File -or -not (Test-Path $File)) {
    exit 0
}

# ── Customize below: add your linter/formatter/test runner ──────────────────

# Example: run ruff on Python files
# if ($File -like "*.py") {
#     & ruff check $File --fix --quiet
#     & ruff format $File --quiet
# }

# ─────────────────────────────────────────────────────────────────────────────

exit 0
