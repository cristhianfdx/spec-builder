# pre-edit.ps1 — runs before Claude edits any file
# Generated by spec-builder

param([string]$InputJson = "")

if (-not $InputJson) {
    $InputJson = $input | Out-String
}

# Walk up to find .specify/
$Root = $null
$Dir = Get-Location
for ($i = 0; $i -lt 5; $i++) {
    if (Test-Path (Join-Path $Dir ".specify")) { $Root = $Dir; break }
    $Parent = Split-Path $Dir -Parent
    if (-not $Parent -or $Parent -eq $Dir) { break }
    $Dir = $Parent
}

if ($Root) {
    $SpecsDir = Join-Path $Root ".specify\specs"
    if (Test-Path $SpecsDir) {
        $Pending = (Select-String -Path (Join-Path $SpecsDir "**\tasks.md") -Pattern "^\- \[ \]" -Recurse -ErrorAction SilentlyContinue | Measure-Object).Count
        if ($Pending -eq 0) {
            Write-Error "Warning: no pending tasks found in .specify\specs\. Validate consistency before editing."
        }
    }
}

exit 0
